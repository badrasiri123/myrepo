---
-
 hosts: all
 become: false
 gather_facts: no
 vars:
         apache_packages:
                 - httpd
                 - git

 tasks:
         - name: install apache
           dnf:
                   name: "{{apache_packages}}"
                   state: present

